use skyzen::routing::{CreateRouteNode, Route, Router};

async fn root() -> &'static str {
    "Hello from {{PACKAGE_NAME}} durable worker!"
}

async fn health() -> &'static str {
    "OK"
}

pub fn router() -> Router {
    Route::new((
        "/".at(root),
        "/health".at(health),
    ))
    .build()
}

